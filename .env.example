# Solana Network Configuration
# Use "devnet" for testing, "mainnet-beta" for production
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Solana RPC Endpoint
# Default devnet RPC: https://api.devnet.solana.com
# For better performance, consider using Helius, QuickNode, or Alchemy
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# WebAuthn (Passkey) Configuration
# Your app's domain (for production, use your actual domain)
# For local dev, use "localhost"
NEXT_PUBLIC_RP_NAME=Lazorkit Passkey Demo
NEXT_PUBLIC_RP_ID=localhost

# Server-side origin for WebAuthn validation
# For local: http://localhost:3000
# For production: https://your-domain.com
NEXT_PUBLIC_ORIGIN=http://localhost:3000

# Lazorkit Sponsor Wallet (Devnet)
# This is a demo wallet that sponsors gasless transactions
# In production, this would be your sponsor service's private key
# IMPORTANT: Never commit real private keys to git!
LAZORKIT_SPONSOR_PRIVATE_KEY=your_base58_encoded_private_key_here

# Optional: Lazorkit API Configuration
# If using Lazorkit's hosted sponsor service (when available)
# LAZORKIT_API_KEY=your_api_key_here
# LAZORKIT_API_URL=https://api.lazorkit.com
